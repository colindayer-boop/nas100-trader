# Edge Hunt Log

Autonomous miner (`edge_hunt.py --all --loop`) results land in `HUNT_LOG.md` and the
robustness verdicts in `SWEEP_SUMMARY.md`.

- prop_fit = OOS Sharpe x %positive-months (ranks by pass-speed AND consistency)
- **Only trust 6/6 robustness passes.**
- Latest sweep: 0/12 robust. EWA/EWC exposed as split-luck.

The daily-yfinance candidate well is **dry**. The real levers now are the
[[07 Deployment|futures data bridge]] and letting the current book confirm.

Back: [[02-Strategy-Research/_index|Strategy Research]]
